@model GisanParkGolf_Core.Components.SearchViewComponent.SearchViewModel

@* 
   이 뷰는 독립적인 UI 조각입니다. 
   폼 태그는 이 컴포넌트를 사용하는 부모 페이지에 있어야 합니다.
   컴포넌트 자체는 검색 UI 요소들만 제공합니다.
*@

<div class="form-check">
    <input type="checkbox" asp-for="ReadyUserOnly" class="form-check-input" />
    <label asp-for="ReadyUserOnly" class="form-check-label">승인대기만 보기</label>
</div>

<div class="input-group mb-3">
    <select asp-for="SearchField" asp-items="Model.SearchFields" class="form-select"></select>
    <input type="text" asp-for="SearchQuery" class="form-control" />
    <button type="submit" class="btn btn-outline-secondary">검색</button>
    <a asp-page="/Admin/PlayerManagement" class="btn btn-primary">초기화</a>
</div>